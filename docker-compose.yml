version: '3'

services:

    mongo:
        image: mongo
        restart: always
    web:
        build: .
        ports:
            - "8000:5000"
        volumes:
            - "./app:/home/kv_store/app"
